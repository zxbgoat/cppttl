#include <stdio.h>

#define MAXN 10000
int need[MAXN], sppt[MAXN];

int main()
{
    int N, M;
    scanf("%d", &N);
    for(int i = 0; i < N; ++i)
        scanf("%d", &need[i]);
    scanf("%d", &M);
    
}
